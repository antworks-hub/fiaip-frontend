import { mapActions, mapGetters } from 'vuex'

export default {
    created() {
    },

    beforeDestroy() {
    },

    computed: {
        ...mapGetters({
        })
    },

    methods: {
        ...mapActions({
        })
    }
}